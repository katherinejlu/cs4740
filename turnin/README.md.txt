# For part 1, run main.py but make sure you have glove in the directory and that it is 6B.100d.txt 
the data path for the csv files is set. so it should run with no adjustment to the code

## For part 2, run bertredo.py but it needs a helper file called utilsmultiplechoice.py from the HuggingFace Tranformers's repository. The data is in a different format so it's in a different directory. 